<%= simple_form_for(list) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, placeholder: "e.g., Action Movies, Classics..." %>
  <%= f.input :photo, as: :file %>
  <div class="d-grid gap-2">
    <%= f.button :submit, submit_label, class: "btn btn-primary" %>
  </div>
<% end %>
